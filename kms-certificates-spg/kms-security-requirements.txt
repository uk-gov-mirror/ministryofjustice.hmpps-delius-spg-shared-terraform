see https://dsdmoj.atlassian.net/browse/DAM-72

                                    jenkins webops	    spg ec2 iso/mpx	    spg ec2 crc
certificates_rootca_kms_key	        Both	Both	    None	            None
certificates_spg_iso_cert_kms_key	Both	Both	    Decrypt	            None
certificates_spg_crc_cert_kms_key	Both	Both	    Decrypt*            Decrypt


* for all in one deployments

Note WebOps is not allowed as a group, however the group has full admin access by default anyway (and assume role for terraform)